@using VCMS.Lib.Models;
@model IEnumerable<Category>

<div class="sidebar-box">
    <div class="box">
        <div class="box-title">
            <h3 class="title-sidebar title-cate-sidebar">Models</h3>
            <div class="box-tool">
                <a data-action="collapse" href="#"><i class="nav-arrow"></i></a>
            </div>
        </div>
        <div class="box-content clearfix">
            <ul class="category-list">
                @foreach (var category in Model)
                {
                    <li class="model-link filter" data-filter=".model-@category.Id" data-content="@category.Id">
                        <a href="#model-@category.Id">@category.Name</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</div>
